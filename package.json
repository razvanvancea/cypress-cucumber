{
  "name": "cypress-cucumber",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cypress:open": "cypress open",
    "cypress:run": "node_modules/.bin/cypress run --browser chrome",
    "cypress:ui": "node_modules/.bin/cypress run --headed",
    "cypress:headless": "node_modules/.bin/cypress run"
  },
  "author": "Razvan Vancea",
  "license": "ISC",
  "dependencies": {
    "cypress-cucumber-preprocessor": "^2.3.1"
  },
  "devDependencies": {
    "cypress": "^4.5.0"
  }
}
